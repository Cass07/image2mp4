# image2mp4
X(구 트위터) AI 사용 방지를 위해 이미지 파일을 1초짜리 mp4 파일로 변환하는 대충만든 웹 애플리케이션

## About
* MediaRecorder 를 사용해서 이미지 파일을 1초 녹화해 이를 mp4 파일로 변환합니다.
* bitrate는 최대 40_000_000 으로 설정했습니다 (올리면 오류나던데 나중에 디버깅함)
* 모든 처리는 브라우저 내에서 진행되며 어떤 데이터도 저장되지 않습니다.